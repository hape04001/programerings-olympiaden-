using System;

namespace upg_1
{
    class Program
    {
        static void Main(string[] args)
        {
            double c42 = (0.229 * 0.324) * 2 ;
            double ansc42 = double.Parse(Console.ReadLine());
            
            double ans42 = c42 * ansc42 ;

            double areaafish = 0.297 * 0.420 * 2;
            double Avikt = double.Parse(Console.ReadLine());
            
            double afishvikt = areaafish * Avikt;

            double A4 = 0.210 * 0.297 ;
            double A4vikt = double.Parse(Console.ReadLine());

            double A4totvikt = A4 * A4vikt;

            Console.Write(A4totvikt + afishvikt +ans42);
        }
    }
}
